/*
Copyright © 2024 Grayson Crozier <grayson40@gmail.com>
*/
package daw

// Stores the path to the .daw directory
const DawDir = ".daw/"

// Stores the path to the info directory
const InfoDir = DawDir + "info/"

// Stores the path to the objects directory
const ObjectsDir = DawDir + "objects/"

// Stores the path to the staged.json file
const StagedPath = ObjectsDir + "staged.json"

// Stores the path to the tracked.json file
const TrackedPath = ObjectsDir + "tracked.json"

// Stores the path to the commits.json file
const CommitsPath = ObjectsDir + "commits.json"

// Stores the path to the user.json file
const UserPath = InfoDir + "user.json"

// Stores the path to the credentials.json file
const CredentialsPath = InfoDir + "credentials.json"
